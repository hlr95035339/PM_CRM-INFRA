# CRM Lite Demo

This is a minimal, self-contained CRM demo that uses only Node built-ins (no external dependencies). It's suitable for uploading to GitHub without `node_modules`.

Run:

```bash
node server.js
# then open http://localhost:3001
```
